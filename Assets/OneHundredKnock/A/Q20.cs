using System;
using OpenCVForUnity.PlotModule;
using UnityEngine;

namespace OneHundredKnock.A
{
    /// <summary>
    /// ヒストグラム表示
    /// OpenCV for Unityでは多分できないし必要性を感じないので飛ばす
    /// </summary>
    public class Q20 : MonoBehaviour
    {
        private void Start()
        {
            //var src = Util.LoadTexture("imori_256x256");
            //var plot = Plot2d.create(src);
            //plot2dクラスを試しに使ってみたらUnityがくらっしゅする
            //plot.setShowGrid(false);
            //GetComponent<Renderer>().material.mainTexture = Util.MatToTexture2D(src);
        }
    }
}